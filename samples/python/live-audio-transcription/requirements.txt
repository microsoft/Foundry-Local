foundry-local-sdk; sys_platform != "win32"
foundry-local-sdk-winml; sys_platform == "win32"
# pyaudio enables live microphone capture. Prebuilt wheels are available on
# Windows and macOS; on Linux it needs the portaudio dev package installed.
# The sample falls back to synthetic audio if pyaudio import fails at runtime.
pyaudio; sys_platform == "win32" or sys_platform == "darwin"
