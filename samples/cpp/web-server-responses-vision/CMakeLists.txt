# This sample is built as part of the C++ SDK.
# See sdk/cpp/CMakeLists.txt for the build target (WebServerResponsesVision).
#
# Build from sdk/cpp:
#   cmake --preset x64-debug
#   cmake --build --preset x64-debug
#
# The built executable will be at:
#   sdk/cpp/out/build/x64-debug/WebServerResponsesVision.exe
