diff --git a/Configurations/10-main.conf b/Configurations/10-main.conf
index f815a09b23..e7c0c8b608 100644
--- a/Configurations/10-main.conf
+++ b/Configurations/10-main.conf
@@ -652,7 +652,7 @@ my %targets = (
         shared_target    => "linux-shared",
         shared_cflag     => "-fPIC",
         shared_ldflag    => sub { $disabled{pinshared} ? () : "-Wl,-znodelete" },
-        shared_extension => ".so.\$(SHLIB_VERSION_NUMBER)",
+        shared_extension => ".so",
         enable           => [ "afalgeng" ],
     },
     "linux-generic64" => {
diff --git a/Configurations/15-android.conf b/Configurations/15-android.conf
index fd5cd3f478..1904374b99 100644
--- a/Configurations/15-android.conf
+++ b/Configurations/15-android.conf
@@ -187,6 +187,7 @@ my %targets = (
         cflags           => add(sub { android_ndk()->{cflags} }),
         cppflags         => add(sub { android_ndk()->{cppflags} }),
         cxxflags         => add(sub { android_ndk()->{cflags} }),
+        shared_ldflag    => add("-Wl,-z,max-page-size=16384"),
         bn_ops           => sub { android_ndk()->{bn_ops} },
         bin_cflags       => "-pie",
         enable           => [ ],
diff --git a/Configurations/unix-Makefile.tmpl b/Configurations/unix-Makefile.tmpl
index 66617d6f4f..668056bded 100644
--- a/Configurations/unix-Makefile.tmpl
+++ b/Configurations/unix-Makefile.tmpl
@@ -66,7 +66,7 @@
      sub shlib {
          my $lib = shift;
          return () if $disabled{shared} || $lib =~ /\.a$/;
-         return $unified_info{sharednames}->{$lib}. $shlibvariant. '$(SHLIB_EXT)';
+         return $unified_info{sharednames}->{$lib}. $shlibvariant. '$(SHLIB_EXT_SIMPLE)';
      }
      sub shlib_simple {
          my $lib = shift;
